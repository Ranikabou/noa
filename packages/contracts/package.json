{
  "name": "@noa/contracts",
  "version": "0.1.0",
  "private": true,
  "main": "./v1/index.ts",
  "types": "./v1/index.ts",
  "scripts": {
    "lint": "eslint v1/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@noa/config": "*",
    "typescript": "^5",
    "vitest": "^2.0.0"
  }
}
